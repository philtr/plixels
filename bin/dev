#!/usr/bin/env ruby

require "bundler/inline"

gemfile do
  source "https://rubygems.org"
  gem "webrick"
end

server = WEBrick::HTTPServer.new(Port: 3456, DocumentRoot: File.expand_path("."))
trap 'INT' do server.shutdown end
server.start

